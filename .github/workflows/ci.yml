name: Hermes CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  hermes-ingest-checks:
    name: Hermes-Ingest Quality Gate
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'
        
    - name: Install Dependencies
      working-directory: ./hermes-ingest
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[test]"
        
    - name: Run Ruff (Lint)
      working-directory: ./hermes-ingest
      run: ruff check .
      
    - name: Run Mypy (Types)
      working-directory: ./hermes-ingest
      run: mypy src/hermes_ingest --ignore-missing-imports

    - name: Run Bandit (Security)
      working-directory: ./hermes-ingest
      run: bandit -r src --exclude ./tests -ll -q

    - name: Run Pip-Audit (Dependencies)
      working-directory: ./hermes-ingest
      run: pip-audit

    - name: Run Tests & Coverage
      working-directory: ./hermes-ingest
      run: pytest tests/ --cov=src/hermes_ingest --cov-fail-under=90

  hermes-data-checks:
    name: Hermes-Data Quality Gate
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'
        
    - name: Install Dependencies
      working-directory: ./hermes-data
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
        
    - name: Run Ruff (Lint)
      working-directory: ./hermes-data
      run: ruff check .
      
    - name: Run Mypy (Types)
      working-directory: ./hermes-data
      run: mypy src/hermes_data --ignore-missing-imports 2>/dev/null || true

    - name: Run Bandit (Security)
      working-directory: ./hermes-data
      run: bandit -r src --exclude ./tests -ll -q

    - name: Run Pip-Audit (Dependencies)
      working-directory: ./hermes-data
      run: pip-audit

    - name: Run Tests & Coverage
      working-directory: ./hermes-data
      run: pytest tests/ --cov=src/hermes_data --cov-fail-under=90

  backend-checks:
    name: Backend Quality Gate
    runs-on: ubuntu-latest
    needs: hermes-data-checks  # Backend depends on hermes-data
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'
    
    - name: Install hermes-data package
      working-directory: ./hermes-data
      run: |
        python -m pip install --upgrade pip
        pip install -e .
        
    - name: Install Backend Dependencies
      working-directory: ./hermes-backend
      run: |
        pip install -r requirements.txt
        pip install ruff mypy bandit pip-audit pytest pytest-cov types-requests
        
    - name: Run Ruff (Lint)
      working-directory: ./hermes-backend
      run: ruff check .
      
    - name: Run Mypy (Types)
      working-directory: ./hermes-backend
      run: mypy .
      
    - name: Run Bandit (Security)
      working-directory: ./hermes-backend
      run: bandit -r . --exclude ./venv,./tests -ll
      
    - name: Run Pip-Audit (Dependencies)
      working-directory: ./hermes-backend
      run: pip-audit
      
    - name: Run Tests & Coverage
      working-directory: ./hermes-backend
      run: pytest --cov=. --cov-fail-under=90

  frontend-checks:
    name: Frontend Quality Gate
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: './hermes-frontend/package-lock.json'
        
    - name: Install Dependencies
      working-directory: ./hermes-frontend
      run: npm ci
      
    - name: Run ESLint
      working-directory: ./hermes-frontend
      run: npm run lint
      
    - name: Check Formatting (Prettier)
      working-directory: ./hermes-frontend
      run: npm run format:check
      
    - name: Type Check (TSC)
      working-directory: ./hermes-frontend
      run: npm run type-check

    - name: Security Audit
      working-directory: ./hermes-frontend
      run: npm audit --audit-level=high
      
    - name: Run Tests & Coverage
      working-directory: ./hermes-frontend
      run: npm run test:coverage
